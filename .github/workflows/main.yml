name: Maintain the hygiene of a repo

on:
  schedule:
#     - cron: "*/1 * * * *"

jobs:
  alert:
    runs-on: ubuntu-latest
    name: hygiene run
    steps:
      - uses: sid-ah/hygiene/actions@master
        env:
          gh_token: "${{ secrets.GH_PERSONAL_ACCESS_TOKEN }}"
